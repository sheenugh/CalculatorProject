const display = document.getElementById("display");

function append_to_display(input) {
  display.value += input;
}

function clear_display() {
  display.value = "";
}

function     calculate() 
